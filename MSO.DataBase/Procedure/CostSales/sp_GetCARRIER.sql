CREATE PROCEDURE [dbo].[sp_GetCARRIER]
AS
SELECT A.CARRIER, A.CARRIERNAME

FROM (

        SELECT RTRIM(VALUE) AS CARRIER, RTRIM(VDESC) CARRIERNAME

        FROM CSOPTFD

        WHERE OPTFIELD = 'CARRIER'

) A

ORDER BY A.CARRIER
Go
